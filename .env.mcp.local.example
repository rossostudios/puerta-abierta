# Copy to .env.mcp.local and fill in your Clerk secret key.
# Clerk MCP requires a Secret Key (not the publishable key).
CLERK_SECRET_KEY=sk_test_replace_me

# Optional: limit available tools. Leave unset for default behavior.
# TOOLS=users,organizations
